class CachedData {
  int weatherCode;
  double tempFeels;
  String weatherMain;
  DateTime date;
  DateTime sunrise;
  DateTime sunset;
  double tempMax;
  double tempMin;
  CachedData({required this.weatherCode, required this.weatherCode,required this.weatherCode,required this.weatherCode,required this.weatherCode,required this.weatherCode,required this.weatherCode,required this.weatherCode})
}
